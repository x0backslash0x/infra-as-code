- name: Labo07
  hosts: host
  gather_facts: true
  become: true
  roles:
    - { role: createuser, username: "{{ user }}", userpass: "{{ userpasswd }}" }
    # - { role: dockerinstall, user: "{{ user }}" }

  vars:
    - user: "dokwerker"
    - userpasswd: "{{ lookup('file', 'host_passwd') }}"

# ansible-playbook playbook-labo07.yml -e user=<host user>
